# 협업 가이드

## 브랜치 전략

| 브랜치 | 용도 |
| --- | --- |
| `main` | 배포 브랜치. `develop`에서만 병합합니다. |
| `develop` | 기본 개발 브랜치. 모든 작업 브랜치의 시작점이자 PR 대상입니다. |
| `<타입>/#이슈번호` | 작업 브랜치. 예: `feat/#1`, `ci/#9` |

작업 브랜치의 타입은 아래 표의 7가지를 사용하며, 이슈 태그·커밋 타입과 항상 동일하게 맞춥니다.

작업은 항상 이슈를 먼저 만들고, 그 이슈 번호로 브랜치를 파서 시작합니다.

```bash
git switch develop
git pull
git switch -c feat/#1
```

## 타입

이슈 태그, 브랜치 접두사, 커밋 타입에 모두 같은 7가지를 사용합니다.

| 타입 | 용도 | 이슈 태그 | 브랜치 | 커밋 |
| --- | --- | --- | --- | --- |
| feat | 기능 추가 | `[FEAT]` | `feat/#1` | `feat:` |
| fix | 버그 수정 | `[FIX]` | `fix/#1` | `fix:` |
| refactor | 동작 변경 없는 코드 개선 | `[REFACTOR]` | `refactor/#1` | `refactor:` |
| chore | 패키지, 설정, 빌드 | `[CHORE]` | `chore/#1` | `chore:` |
| ci | 워크플로우, 배포 파이프라인 | `[CI]` | `ci/#1` | `ci:` |
| docs | 문서 | `[DOCS]` | `docs/#1` | `docs:` |
| design | 기능 변경 없는 UI, 스타일 | `[DESIGN]` | `design/#1` | `design:` |

## 커밋 메시지

```
<타입>: <한 줄 요약>
```

```
feat: 로그인 폼 유효성 검사 추가
fix: 새로고침 시 토큰이 사라지는 문제 수정
refactor: 유저 정보 조회 로직 분리
chore: eslint 설정 추가
ci: PR 빌드 워크플로우 추가
docs: README 실행 방법 보강
design: 헤더 반응형 여백 조정
```

요약은 한글로, 마침표 없이, 무엇을 했는지가 드러나게 씁니다.

## 이슈와 PR

제목은 대괄호 태그로 시작합니다.

- 이슈: `[FEAT] 로그인 화면 구현`, `[CI] PR 빌드 워크플로우 추가`
- PR: 이슈 제목과 동일하게 맞춥니다.

PR 본문에 `close #이슈번호`를 적으면 병합 시 이슈가 자동으로 닫힙니다.

## PR 규칙

- 대상 브랜치는 `develop`입니다.
- 리뷰 승인 1개 이상을 받은 뒤 병합합니다.
- 병합은 PR 작성자가 직접 합니다.
- 병합된 작업 브랜치는 삭제합니다.
