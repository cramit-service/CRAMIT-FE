// src/app/(main)/projects/page.tsx
import { LectureListScreen } from '@/features/study/components/LectureListScreen';

// 학습하기(내 강의 / 공유 강의 목록) 페이지. 얇게 유지하고 조립만 한다.
export default function ProjectListPage() {
  return <LectureListScreen />;
}
